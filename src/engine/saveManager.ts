/**
 * saveManager.ts — 存档管理器（阶段 1 占位）
 *
 * 后续阶段将实现：
 * - 单一自动存档 LocalStorage 读写
 * - 关键选择确认后即刻保存并锁定
 * - 自动保存节点触发（章节开始/关键选择/章节结束/进入结局前）
 * - 存档校验与损坏恢复
 *
 * LocalStorage 键名：
 *   snow-before-v1-save
 */
export class SaveManager {
  // 占位：阶段 4 实现
}
