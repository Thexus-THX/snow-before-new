/**
 * historyManager.ts — 历史面板与回滚管理器（阶段 1 占位）
 *
 * 后续阶段将实现：
 * - HistoryEntry 生成与追加
 * - 历史面板渲染（从右侧滑出）
 * - 普通选择回滚（恢复完整状态快照）
 * - 关键选择已锁定显示
 * - 隐藏数值、路线倾向不暴露
 */
export class HistoryManager {
  // 占位：阶段 4 实现
}
