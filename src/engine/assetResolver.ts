/**
 * assetResolver.ts — 资源解析器（阶段 1 占位）
 *
 * 后续阶段将实现：
 * - 素材路径规范化为 public/assets/ 相对路径
 * - 缺失素材检测与占位块生成（灰色虚线边框 + 文件路径）
 * - 图片懒加载与预加载策略
 * - 音频缺失时仅 console.warn，不阻断剧情
 */
export class AssetResolver {
  // 占位：阶段 8 实现
}
