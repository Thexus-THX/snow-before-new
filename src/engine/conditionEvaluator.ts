/**
 * conditionEvaluator.ts — 条件求值器（阶段 1 占位）
 *
 * 后续阶段将实现：
 * - ConditionGroup 的 all / any / none 组合求值
 * - statMin、statMax、prepMin、trustMin 等条件判断
 * - flag 存在/不存在检查
 * - lockedHint 叙事化显示
 */
export class ConditionEvaluator {
  // 占位：阶段 3 实现
}
